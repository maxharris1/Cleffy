## Summary

A PDF with a cover, blank, or front-matter page used to fail the whole book: Audiveris flagged those sheets as invalid and refused to export the rest. Cover pages are now skipped and the remaining music is transcribed. The transport discloses `pages_skipped` instead of an `omr_crash`.

Rebased onto `feat/musical-playback` from `mh/skip-non-musical-omr-pages-6a3f` (that original fork of `main` can be deleted). Engine is `audiveris-5.11.0+svc-12` so cached analyses re-run.

PR #31 (Moonlight OMR repairs) also claims svc-12. Whichever of these merges second should bump to svc-13.

## Test plan

- [ ] omr-service: `npm test -- src/job.test.ts src/audiveris.test.ts`
- [ ] Transport discloses "Some pages had no readable music and were skipped."
- [ ] Optional: run a PDF with a title page through local OMR and confirm the book completes
