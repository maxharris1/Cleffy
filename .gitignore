# dependencies / build
node_modules/
dist/
dev-dist/
coverage/

# environment — never commit real keys
.env
.env.*
!.env.example
# .env.production is client-safe by design and is what the Vercel build reads.
!.env.production

# supabase CLI local state
supabase/.temp/
supabase/.branches/

# editor / os
.DS_Store
*.local

# E2E artifacts
live-*.png
verify-*.png
e2e-shots/

# local secrets (service accounts, etc.)
.secrets/
